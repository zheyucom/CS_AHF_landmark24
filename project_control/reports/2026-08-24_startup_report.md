# 项目正式启动报告

日期：2026-08-24

## 已完成

- 使用 Ponytail 将项目压缩为三阶段和明确阶段门。
- 使用 Zotero Taxonomy Curator 校验 22 个笔记路径，0 warning。
- 核对首批 15 篇文献卡：15 篇有原文核验，14 篇标记全文完成。
- 增量同步 Zotero：R020 新增 35 条批注；R055 新增 33 条批注并更新
  1 条子笔记；同步状态已写回。
- 对 v2 CSV 做全量只读 EDA：2,424 行、318 列、334 events、0 个重复行。
- 修复分析脚本未遵循 `config_v2.yaml` 输出路径的问题。
- 补跑 v2 071、077、077B、078A、078，生成缺失的最终特征、敏感性和错误审计。

## 诊断结果

- v2 最终候选集中 66 个可用特征，61 个系数非零。
- 30 次主结局敏感性重跑：AUROC 0.7614，AUPRC 0.3459，Brier 0.1062。
- 12-48 h、NEE 0.10、不含 NEE、排除 pre12 shock 等方向与既有结论一致。
- 固定测试集错误组：TP 22、FP 75、FN 45、TN 343。
- 这些结果用于诊断和设计冻结，不作为论文最终结果。

## 修复的问题

1. 073、074、077、078A 曾固定读取 v1 `outputs/tables`，现改为读取 config 的
   `table_dir`。
2. 078 曾默认读取/写入 v1 目录，现改为读取 config 的 `model_dir/table_dir`。
3. 外部事件时间 CSV 的显式列名此前会错误地先在主表查找，现已修复。
4. 077B 模型名固定写成 68/59 features，现按实际特征数动态命名。

## v3 完成状态

- `.pgpass` 认证已就绪，使用本地 `postgres@mimiciv31` 完成非交互运行。
- 从 Navicat 恢复并按 SHA-256 归档 010-070G 必需 SQL；24 h 旧分支、空文件、
  inspection-only SQL 和被 070D2 替代的 070D 未进入主链。
- 在全新 `study_ahf_v3` schema 中一次通过 010-081A 全链和数据库内审计。
- v3 与历史 cohort flow 各步行数一致；080G 为 2,424 行、318 列、334 events，
  079A 标签与事件时间不一致数为 0。
- 完整 Python 分析已写入 `outputs_v3/`，共 261 个工件。

## 结论

MIMIC 开发阶段的隔离 v3 全链重跑已完成，`study_ahf_v3`、`data/raw_v3/` 和
`outputs_v3/` 现为后续论文写作与本院外部验证的事实源。全项目仍需等待本院
脱敏数据后完成阶段 3 外部验证。
